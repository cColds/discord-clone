export default function DmMainChat() {
  // Loop through all messages from descending order (maybe 20 msgs limit) and style them in a message container list

  return <ol className="mb-4 min-h-[150px]"></ol>;
}
